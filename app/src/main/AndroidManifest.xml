<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.gre">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Invaliddata"></activity>
        <activity android:name=".Validate" />
        <activity android:name=".Department" />
        <activity android:name=".Received" />
        <activity android:name=".Registereddata" />
        <activity android:name=".Check_status" />
        <activity android:name=".Complaints" />
        <activity android:name=".Home" />
        <activity android:name=".Adminhome" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>